# Workspace Rules

- **Commit Messages**: Write commit messages like a human. Keep them conversational, descriptive, and natural (e.g., "Clean up global styles and set up dark mode" or "Make the navbar look beautiful and sticky") instead of strictly using dry conventional prefixes like `feat:` or `fix:`.
